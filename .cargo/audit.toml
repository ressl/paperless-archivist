[advisories]
# `rsa` is present only through SQLx's optional MySQL/macro dependency metadata in
# Cargo.lock. The workspace builds SQLx with postgres + migrate only; `cargo tree
# --invert rsa` has no active path from any workspace crate. Keep this ignore
# scoped to the advisory and revisit when SQLx publishes a lockfile-clean path.
ignore = ["RUSTSEC-2023-0071"]

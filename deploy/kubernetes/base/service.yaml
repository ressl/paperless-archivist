apiVersion: v1
kind: Service
metadata:
  name: paperless-archivist
  labels:
    app.kubernetes.io/name: paperless-archivist
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: paperless-archivist
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http

# Issue #360: i18n Structural CI Gate Implementation Plan

**Goal:** Make missing locale files, parser failures, missing translation keys, and placeholder mismatches fail locally and in the authoritative shared-blueprint GitLab pipeline while keeping identical loanwords non-blocking and explicit.

### Task 1: Capture the false-green behavior with subprocess fixtures

**Files:**
- Create: `scripts/verify/i18n_translation_parity.test.mjs`
- Modify: `frontend/package.json`

- [ ] Build isolated temporary fixture repositories and execute the production checker as a subprocess.
- [ ] Prove a valid inventory exits zero.
- [ ] Prove missing-file, parse-error, missing-key, and placeholder-mismatch fixtures currently false-green and specify stable diagnostic classes with locale/key context.

### Task 2: Make structural drift fail closed

**Files:**
- Modify: `scripts/verify/i18n_translation_parity.mjs`

- [ ] Support an explicit fixture root while retaining the repository root as the default.
- [ ] Distinguish file-read, parser, missing-key, extra-key, and placeholder errors in diagnostics.
- [ ] Exit non-zero when any structural error exists; keep identical values as warnings and document explicitly allowed technical/product terms.
- [ ] Keep the cleaned current translation inventory green.

### Task 3: Wire the gate into both CI paths

**Files:**
- Modify: `frontend/package.json`
- Modify: `.gitlab-ci.yml`

- [ ] Run the subprocess regression suite through `pnpm test`, which the authoritative shared blueprint executes as `node:test`.
- [ ] Run the structural checker through `pnpm lint`, which the authoritative shared blueprint executes as `node:lint`.
- [ ] Retain the explicit public `frontend:i18n` job and update stale comments so public/exported CI remains self-contained.

### Task 4: Verify and deliver

- [ ] Run all fixture tests, the real inventory check, full frontend tests, lint/contracts, typecheck, build, CI-config validation, and `git diff --check`.
- [ ] Obtain independent review, commit, push, prove `node:test` and `node:lint` execute the new gate remotely, and close #360 only after both branch and MR pipelines are green.
